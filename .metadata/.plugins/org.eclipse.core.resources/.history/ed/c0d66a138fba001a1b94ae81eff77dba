package sef.ATestTask.FirstActivity;
import java.util.*;
public class FirstActivity {

	public static void main(String[] args) {
		Map<Integer, Employee> employeemap = new HashMap<Integer, Employee>(); 
		
		Employee e1 = new Employee(1, "Auditor", "Java", 2000);
		Employee e2 = new Employee(2, "Realtor", "House", 1500);
		Employee e3 = new Employee(3, "Programmer", "JavaC", 3000);
		Employee e4 = new Employee(4, "Architect", "Palace", 25000);
		Employee e5 = new Employee(5, "Chef", "Fast", 3500);
		Employee e6 = new Employee(6, "Designer", "Beauty", 1800);
		Employee e7 = new Employee(7, "Engeneer", "Create", 39000);
		
		employeemap.put(e1.getEmpId(), e1);
		employeemap.put(e2.getEmpId(), e2);
		employeemap.put(e3.getEmpId(), e3);
		employeemap.put(e4.getEmpId(), e4);
		employeemap.put(e5.getEmpId(), e5);
		employeemap.put(e6.getEmpId(), e6);
		employeemap.put(e7.getEmpId(), e7);
		//TODO 1 Create collection of employee (more than 5) list or map
		
		Set<Map.Entry<Integer, Employee>> entries = employeemap.entrySet();
	
		
	new FirstActivity().print(employeemap);
}
	void print(Map map)	{
		//3 - Type code to print this map
		Set keySet = map.keySet();
		System.out.println("******************************");
		System.out.println("Key\tValue");
		Iterator it = keySet.iterator();
		while (it.hasNext())
		{
			String key = (String)it.next();
			System.out.println(key+"\t"+map.get(key));
		}
		System.out.println("******************************");
	}
	

		//TODO 2 sort and this employees by salary (from min to max)
		// TIP - google bubble sort
		//		System.out.println() result

		//TODO 3 create instance of a Student as a Person ->
		// than ask him introduce()
		// than make them Student
		// than ask him introduce()

		//TODO 4 Create method for full change of employee information
		// for example some employee change his work




}
	
